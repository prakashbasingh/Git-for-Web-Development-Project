<!-- reveiw questions to Submit in today's file firt-lastname.txt --> 
<!--
Copy the questions below into the firt-lastname.txt file on your github repo - answer each question and then push your changes. 

    1. What is Semantic HTML? 
symantic HTML provides meaning to our content such as header, footer, section etc.
    2. What is HTML used for? 
HTML is used to build web sites.
    3. What is an attribute and where do we put it?
 Attributes is a extra data that we can supply to an element to give some behaviors. examples of attributes are attr, herf, src.
Attributes always goes inside the open tag.
    4. What is the h1 tag used for? How many times should I use it on a page?
h1 is for main heading and should only use once.
    5. Name two tags that have required attributes
<img>, <a>
    6. What do we put in the head of our HTML document?
 Title of the page.
    7. What is an id? 
"id" is a attribute and a unique identifier for specific element and id name is used only once. very specific. 
    8. What elements can I add an id to? 
"id" can be used on any HTML element.
    9. How many times can I use the same id on a page?
 Just once.
    10. What is a class?
 Class is an attribute that classify one or more elements in HTML. not specific
    11. What elements can I add a class to? 
it can also be used on any element.

    12. How many times can I use the same class on a page? 
as many as we want if we want to give same properties.
    13. How do I get my link to open in a new tab?
using anchor tag and href attriute
<a href ="http://wwww.......">click here</a>
    14. What is the alt attribute used for?
 alt attribute specifies alternate text for an image, if image is not displayed.
    15. How do I reference an id?
#idName{

}
    16. What is the difference between a section and a div
The section tag defines sections in a document like chapters, headers, footers whereas div tag defines a division in an HTML document
    17. What is CSS used for? 
CSS discribes how HTML elements are displayed on the page. it give various properties such as color, font-size, font type.
    18. How to we select an element? Example - every h2 on the page
h2{

}
    19. What is the difference between a class and an id? - Give me an example of when I might use each one
class can be used for multiple elements that are similar such as paragraphs. Whereas id is specific and can only be used for one element. id is used for those element which we want to have a unique properties. 
    20. How do we select classes in CSS?
.className{
}
    21. How do we select a p element with a single class of “human””?
.human{
}
    22. What is a parent child selector? When would this be useful? 
child inherits personalty and triats of parants meaning whatever propertiese we give to parents children will have the same properties. child are nested inside the parent element.
    23. How do you select all links within a div with the class of sidebar?
div class a{

}
    24. What is a pseudo selector?

    25. What do we use the change the spacing between lines?
<p>........</p>
    26. What do we use to change the spacing between letters?
......{
letter-spacing: ..px;
}
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
...{
text-transform: capitalize;
text-transform: lowercase;
test-tranform: capitalize;
}
    28. How do I add a 1px border around my div that is dotted and black?
div{
  border: 1px solid black
}
    29. How do I select everything on the page? 
*{

}
    30. How do I write a comment in CSS?
/*......*/
    31. How do I find out what file I am in, when I am using the command line?
 pwd
    32. Using the command line - how do I see a list of files/folders in my current folder?
ls
    33. How do I remove a file via the command line? Why do I have to be careful with this? 
rm fileName. it will dlet permanently and we can not retrive it. 
    34. Why should I use version control? 
- it allow multiple redraft and revision.
- it tracks every change made by many contributors.

    35. How often should I commit to github?
every 20 minutes or whenever we finish making changes.
    36. What is the command we would use to push our repo up to github? 
git push -u origin fileName
    37. Walk me through Lambda's git flow. 

Stretch Questions

    1. What is the difference between an inline element and a block element?

Inline elemen do not creat a new line where as block element can display multiple lines. 
    2. What happens when an element is positioned absolutely? 

It will put element in top left of the parent element.

    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 

elementName{
width: ...px;
height:...px;
}
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
- 3 elements that are display block by default: <p>, <div>, <section>
- 2 elements that are display inline by default: <span>, <a>
- 1 element that is display inline-block by default: <nav>

    5. In your own words, explain the box model. What is the fix for the box model? 
in HTML box model represent space for content/element and surrounding space which wraps arround the elemnt. The box model always has  content(text or image), padding (clears area around contnt,transparent), border (Arround padding), and margin (clears area around border, transparent). 
